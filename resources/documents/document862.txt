static void   devinit snd atiixp proc init struct atiixp  chip 
{
	struct snd info entry  entry 

	if    snd card proc new chip card   atiixp    entry  
		snd info set text ops entry  chip  snd atiixp proc read  
}

