static int snd sonicvibes capture trigger struct snd pcm substream  substream 
					  int cmd 
{
	struct sonicvibes  sonic = snd pcm substream chip substream  
	return snd sonicvibes trigger sonic  2  cmd  
}

