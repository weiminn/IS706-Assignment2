   
   snd pcm format big endian   Check the PCM format is big endian
   @format  the format to check
  
   Returns 1 if the given PCM format is big endian  0 if
   little endian  or a negative error code if endian not specified 
   
int snd pcm format big endian snd pcm format t format 
{
	int val 

	val = snd pcm format little endian format  
	if  val   0 
		return val 
	return  val 
}

