                                                                   
static int snd als4000 playback open struct snd pcm substream  substream 
{
	struct snd sb  chip = snd pcm substream chip substream  
	struct snd pcm runtime  runtime = substream runtime 

	chip playback substream = substream 
	runtime hw = snd als4000 playback 
	return 0 
}

